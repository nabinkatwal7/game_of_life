import React from 'react';
import {FC, useState} from "react";
import './App.css';

const numRows = 25;
const numCols = 35;
const App: FC = () => {
    const [grid, setGrid] = useState()
}

export default App;
